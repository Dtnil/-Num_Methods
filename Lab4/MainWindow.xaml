<Window x:Class="InterpolationApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Інтерполяція: Поліном Лагранжа та Кубічний Сплайн"
        Height="820" Width="1200"
        Background="#1E1E2E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6D28D9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#5B21B6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#313244"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#89B4FA"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#181825" CornerRadius="12" Padding="20,14" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Лабораторна робота №4" Foreground="#6C7086" FontSize="12"/>
                    <TextBlock Text="Поліном Лагранжа та Кубічний Сплайн" 
                               Foreground="#CDD6F4" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="10" Height="10" Fill="#A6E3A1" Margin="0,0,6,0"/>
                    <TextBlock Text="ЧМІ · Інтерполяція" Foreground="#6C7086" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Controls Panel -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Варіант:" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                <ComboBox Grid.Column="1" x:Name="cmbVariant" Style="{StaticResource ComboBoxStyle}"
                          SelectionChanged="CmbVariant_SelectionChanged"/>

                <TextBlock Grid.Column="2" Text="Тип сплайну:" Style="{StaticResource LabelStyle}" Margin="16,0,8,0"/>
                <ComboBox Grid.Column="3" x:Name="cmbSplineType" Style="{StaticResource ComboBoxStyle}">
                    <ComboBoxItem Content="Зімкнений" IsSelected="True"/>
                    <ComboBoxItem Content="Природній"/>
                </ComboBox>

                <TextBlock Grid.Column="4" Text="S'(x₀):" Style="{StaticResource LabelStyle}" Margin="16,0,8,0"/>
                <Border Grid.Column="5" Background="#45475A" CornerRadius="6" BorderBrush="#585B70" BorderThickness="1">
                    <TextBox x:Name="txtDx0" Text="0.1"
                             Background="Transparent" Foreground="#CDD6F4" BorderThickness="0"
                             Padding="8,6" FontSize="13" CaretBrush="#CDD6F4"/>
                </Border>

                <TextBlock Grid.Column="6" Text="S'(xₙ):" Style="{StaticResource LabelStyle}" Margin="16,0,8,0"/>
                <Border Grid.Column="7" Background="#45475A" CornerRadius="6" BorderBrush="#585B70" BorderThickness="1">
                    <TextBox x:Name="txtDxn" Text="-0.5"
                             Background="Transparent" Foreground="#CDD6F4" BorderThickness="0"
                             Padding="8,6" FontSize="13" CaretBrush="#CDD6F4"/>
                </Border>

                <Button Grid.Column="9" Content="▶  Обчислити" Style="{StaticResource ModernButton}"
                        Click="BtnCompute_Click" Margin="8,0,0,0"/>
                <Button Grid.Column="10" Content="↺ Скинути" Click="BtnReset_Click"
                        Margin="8,0,0,0" Padding="12,8"
                        Background="#45475A" Foreground="#CDD6F4" BorderThickness="0"
                        FontSize="13" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Info & Points -->
            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                <!-- Variant Info -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Вузлові точки" Foreground="#A6ADC8" FontSize="11" 
                                   FontWeight="Bold" Margin="0,0,0,10"/>
                        <DataGrid x:Name="dgPoints" AutoGenerateColumns="False"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#1E1E2E" AlternatingRowBackground="#252535"
                                  Foreground="#CDD6F4" GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#45475A"
                                  HeadersVisibility="Column" CanUserAddRows="False"
                                  FontSize="13">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#181825"/>
                                    <Setter Property="Foreground" Value="#89B4FA"/>
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="i" Binding="{Binding Index}" Width="35"/>
                                <DataGridTextColumn Header="x" Binding="{Binding X}" Width="*"/>
                                <DataGridTextColumn Header="y" Binding="{Binding Y}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Computed Values Table -->
                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Значення в точках" Foreground="#A6ADC8" FontSize="11"
                                   FontWeight="Bold" Margin="0,0,0,10"/>
                        <DataGrid x:Name="dgValues" AutoGenerateColumns="False"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="#1E1E2E" AlternatingRowBackground="#252535"
                                  Foreground="#CDD6F4" GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#45475A"
                                  HeadersVisibility="Column" CanUserAddRows="False"
                                  MaxHeight="200" FontSize="11">
                            <DataGrid.ColumnHeaderStyle>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#181825"/>
                                    <Setter Property="Foreground" Value="#89B4FA"/>
                                    <Setter Property="Padding" Value="6,4"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="11"/>
                                </Style>
                            </DataGrid.ColumnHeaderStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="x" Binding="{Binding X}" Width="55"/>
                                <DataGridTextColumn Header="Lagrange" Binding="{Binding Lagrange}" Width="*"/>
                                <DataGridTextColumn Header="Spline" Binding="{Binding Spline}" Width="*"/>
                                <DataGridTextColumn Header="Δ" Binding="{Binding Diff}" Width="55"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Status -->
                <Border x:Name="borderStatus" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Статус" Foreground="#A6ADC8" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock x:Name="txtStatus" Text="Оберіть варіант та натисніть Обчислити"
                                   Foreground="#6C7086" FontSize="12" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Right Panel: Charts -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="220"/>
                </Grid.RowDefinitions>

                <!-- Main Chart -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Графік інтерполяції" Foreground="#CDD6F4" FontSize="15" FontWeight="Bold"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Width="12" Height="3" Fill="#89B4FA" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Лагранж" Foreground="#89B4FA" FontSize="12" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                <Ellipse Width="12" Height="3" Fill="#A6E3A1" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Сплайн" Foreground="#A6E3A1" FontSize="12" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                <Ellipse Width="8" Height="8" Fill="#F38BA8" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Вузли" Foreground="#F38BA8" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <Canvas Grid.Row="1" x:Name="canvasMain" Background="#181825" ClipToBounds="True"
                                SizeChanged="Canvas_SizeChanged"/>
                    </Grid>
                </Border>

                <!-- Error Chart -->
                <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Різниця: Лагранж − Сплайн (невузлові точки)" 
                                       Foreground="#CDD6F4" FontSize="15" FontWeight="Bold"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Ellipse Width="12" Height="3" Fill="#FAB387" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Δ(x) = L(x) − S(x)" Foreground="#FAB387" FontSize="12" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                        <Canvas Grid.Row="1" x:Name="canvasDiff" Background="#181825" ClipToBounds="True"
                                SizeChanged="Canvas_SizeChanged"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
